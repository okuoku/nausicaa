;; compile-all.sps -
;;

(import
  (only (glibc cstring))
  (only (glibc environment))
  (only (glibc streams))
  (only (glibc streams-unlocked))
  (only (glibc streams-extended))
  )

;;; end of file
