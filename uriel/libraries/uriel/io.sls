;; io.sls --
;;

;;page
;; ------------------------------------------------------------
;; Setup.
;; ------------------------------------------------------------


(library (uriel io)
	 (export with-output-to-string)
	 (import (rnrs))

;; ------------------------------------------------------------

;;page
;; ------------------------------------------------------------
;; String ports.
;; ------------------------------------------------------------

(define-syntax with-output-to-string
  (syntax-rules ()
    [(_ ?form ...)
     (call-with-string-output-port
      (lambda () ?form ...))]))

;; ------------------------------------------------------------

;;page
;; ------------------------------------------------------------
;; Done.
;; ------------------------------------------------------------

) ;; end of library form

;;; end of file
